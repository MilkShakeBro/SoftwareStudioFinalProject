query VocabularySetsQuery {
    vocabularySets {
        id
        name
        image
        vocabularies {
            id
            word
            chinese
            breakpoint
        }
        createdAt
        updatedAt
    }
}