query ArticleQuery($articleId: Int!) {
    article(id: $articleId) {
        id
        name
        image
        answers {
            id
            answer_id
            answer_string
        }
        questions {
            id
            question_id
            question_string
        }
        content
        createdAt
        updatedAt
    }
}